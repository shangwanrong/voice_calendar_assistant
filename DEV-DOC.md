# 语音日历微信小程序 — 开发文档

---

## 一、项目概述

### 1.1 产品定位
一款以**语音交互为核心**的微信小程序日历管理工具，让用户通过自然语言语音指令即可完成事件的添加、删除、查看和提醒管理，大幅降低日历操作门槛，提升效率。

### 1.2 目标用户
- 日程繁忙、需要快速记录事件的职场人士
- 不便手动输入的场景（开车、做饭、运动等）
- 中老年用户或对手动输入不熟练的群体

### 1.3 核心价值
- **语音优先**：一句话完成事件创建，无需手动填写表单
- **智能解析**：自动识别时间、日期、事件内容
- **即时反馈**：语音播报确认结果，形成完整闭环

---

## 二、功能模块设计

### 2.1 语音交互模块（核心）

| 功能 | 语音示例 | 说明 |
|------|---------|------|
| 添加事件 | "明天下午3点开会" | 自动解析日期+时间+事件名 |
| 删除事件 | "删除明天的开会" | 按事件名/时间模糊匹配 |
| 查看事件 | "今天有什么安排" | 查询指定日期的事件列表 |
| 修改事件 | "把开会改到4点" | 修改已有事件的时间 |
| 设置提醒 | "开会前15分钟提醒我" | 为事件添加提醒 |

### 2.2 日历展示模块

| 功能 | 说明 |
|------|------|
| 月视图 | 默认视图，显示当月日期网格，有事件的日期标注圆点 |
| 日视图 | 展示某天的所有事件列表，支持滑动切换日期 |
| 事件卡片 | 显示事件名称、时间、提醒状态，支持左滑删除 |

### 2.3 事件管理模块

| 功能 | 说明 |
|------|------|
| 手动添加 | 表单输入：事件名、日期、时间、提醒方式 |
| 手动编辑 | 修改事件的任意字段 |
| 手动删除 | 滑动删除或长按删除 |
| 事件搜索 | 按关键词搜索历史事件 |

### 2.4 提醒通知模块

| 功能 | 说明 |
|------|------|
| 小程序内提醒 | 进入小程序时显示未读提醒 |
| 微信服务通知 | 通过微信服务通知推送提醒（需订阅消息） |
| 提醒时间设置 | 支持事件前5/10/15/30/60分钟提醒 |

### 2.5 语音反馈模块

| 功能 | 说明 |
|------|------|
| 语音播报 | 操作完成后语音播报结果（如"已添加明天下午3点的开会"） |
| 语音确认 | 模糊匹配时语音确认（如"你说的是明天下午3点的开会吗？"） |
| 错误提示 | 语音提示解析失败，引导用户重新输入 |

---

## 三、技术架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                 微信小程序前端                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ 语音录制  │ │ 日历UI   │ │ 事件管理UI       │ │
│  │  + 播报   │ │ 月/日视图 │ │ 添加/编辑/删除   │ │
│  └─────┬────┘ └─────┬────┘ └────────┬─────────┘ │
│        │            │               │            │
│  ┌─────▼────────────▼───────────────▼─────────┐  │
│  │            小程序逻辑层 (JS)                 │  │
│  │  ┌────────────┐  ┌───────────────────────┐  │  │
│  │  │ 语音识别    │  │ NLP 时间/意图解析      │  │  │
│  │  │ wx.getRecorderManager / │ 本地规则引擎   │  │  │
│  │  │ wx.cloud.callFunction   │ + 云函数AI    │  │  │
│  │  └────────────┘  └───────────────────────┘  │  │
│  └──────────────────────┬──────────────────────┘  │
│                         │                         │
│  ┌──────────────────────▼──────────────────────┐  │
│  │            数据存储层                         │  │
│  │  ┌──────────────┐  ┌─────────────────────┐  │  │
│  │  │ 本地缓存      │  │ 云开发数据库         │  │  │
│  │  │ wx.setStorage │  │ db.collection('events')│ │
│  │  └──────────────┘  └─────────────────────┘  │  │
│  └─────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

### 3.2 技术选型

| 类别 | 技术方案 | 说明 |
|------|---------|------|
| 前端框架 | 微信小程序原生 | 稳定、兼容性好、无需额外框架 |
| 语音识别 | wx.createRecognizerContext()（内置语音识别API） | 无需插件，微信基础库2.23.0+原生支持，免费无限制 |
| 语音播报 | 云函数 + 百度TTS API / 文字反馈兜底 | 插件TTS已不可用，MVP阶段用文字反馈，正式版接入百度TTS |
| NLP意图解析 | 本地规则引擎 + 云函数AI增强 | 简单指令本地解析，复杂语义走云函数 |
| 数据存储 | 微信云开发（云数据库 + 云函数） | 免服务器、免域名备案、天然鉴权 |
| 提醒推送 | 微信订阅消息 | 小程序唯一合规的推送方式 |
| UI组件库 | Vant Weapp / 自定义组件 | 日历组件可自定义，其他用Vant加速 |

### 3.3 语音识别方案对比

| 方案 | 优点 | 缺点 | 费用 |
|------|------|------|------|
| 微信同声传译插件 | 集成简单、免费额度 | 识别精度一般、中文支持有限 | 免费（有限额） |
| 讯飞语音识别（云函数） | 中文识别精度高、支持方言 | 需要注册开发者账号 | 每日500次免费 |
| 百度语音识别（云函数） | 中文识别好、文档完善 | 需要注册开发者账号 | 每日50000次免费 |
| 腾讯云语音识别 | 与云开发生态一致 | 需要开通腾讯云服务 | 每月15小时免费 |

**推荐方案**：MVP阶段使用**微信同声传译插件**快速验证，正式版切换至**百度语音识别**（免费额度最大）。

---

## 四、数据结构设计

### 4.1 事件集合 (events)

```json
{
  "_id": "自动生成",
  "openid": "用户唯一标识",
  "title": "开会",
  "date": "2026-05-31",
  "startTime": "15:00",
  "endTime": "16:00",
  "reminder": 15,
  "reminderUnit": "minute",
  "isAllDay": false,
  "category": "work",
  "note": "三楼会议室",
  "createdAt": "2026-05-30T10:00:00.000Z",
  "updatedAt": "2026-05-30T10:00:00.000Z"
}
```

### 4.2 语音操作日志集合 (voice_logs)

```json
{
  "_id": "自动生成",
  "openid": "用户唯一标识",
  "rawText": "明天下午3点开会",
  "intent": "add_event",
  "parsedResult": {
    "title": "开会",
    "date": "2026-05-31",
    "startTime": "15:00"
  },
  "success": true,
  "createdAt": "2026-05-30T10:00:00.000Z"
}
```

### 4.3 本地缓存结构

```json
{
  "calendar_events": "事件列表缓存（减少云数据库请求）",
  "calendar_settings": "用户偏好设置",
  "calendar_last_sync": "最后同步时间戳"
}
```

---

## 五、NLP 语义解析设计

### 5.1 意图分类

| 意图 | 关键词/模式 | 示例 |
|------|-----------|------|
| add_event | 添加/新增/安排/定/有 + 时间词 | "明天下午3点开会" |
| delete_event | 删除/取消/去掉 + 事件词 | "删除明天的开会" |
| query_event | 今天/明天/这周 + 什么/安排/日程 | "今天有什么安排" |
| modify_event | 修改/改/调整 + 事件词 | "把开会改到4点" |
| set_reminder | 提前/之前 + 时间 + 提醒 | "开会前15分钟提醒我" |

### 5.2 时间解析规则

```
相对时间:
  今天/明天/后天/大后天 → 计算具体日期
  下周一/下周五 → 计算具体日期
  这周末 → 计算具体日期

时间段:
  早上/上午 → 8:00-11:59
  中午 → 12:00-13:59
  下午 → 14:00-17:59
  晚上 → 18:00-22:59

具体时间:
  3点/3:00 → 15:00（下午语境）或 03:00（凌晨语境）
  3点半 → 15:30 / 03:30
  差一刻4点 → 15:45
```

### 5.3 解析流程

```
语音输入 → ASR转文字 → 意图分类 → 实体提取(时间/事件名) → 执行操作 → 语音反馈
                              ↓
                        置信度低 → 语音确认 → 用户确认/修正 → 执行操作
```

---

## 六、页面结构设计

```
pages/
├── index/              # 首页 - 月历视图 + 语音按钮
│   ├── index.wxml
│   ├── index.wxss
│   ├── index.js
│   └── index.json
├── day/                # 日视图 - 当日事件列表
│   ├── day.wxml
│   ├── day.wxss
│   ├── day.js
│   └── day.json
├── event-detail/       # 事件详情/编辑
│   ├── event-detail.wxml
│   ├── event-detail.wxss
│   ├── event-detail.js
│   └── event-detail.json
├── event-add/          # 手动添加事件
│   ├── event-add.wxml
│   ├── event-add.wxss
│   ├── event-add.js
│   └── event-add.json
└── settings/           # 设置页
    ├── settings.wxml
    ├── settings.wxss
    ├── settings.js
    └── settings.json

components/
├── calendar/           # 月历组件
├── voice-btn/          # 语音按钮组件（悬浮）
├── event-card/         # 事件卡片组件
└── voice-feedback/     # 语音反馈动画组件

cloudfunctions/
├── voice-parse/        # 语音识别 + NLP解析云函数
├── event-manage/       # 事件增删改查云函数
└── reminder-check/     # 提醒检查云函数（定时触发）
```

---

## 七、核心交互流程

### 7.1 语音添加事件流程

```
用户点击语音按钮
    ↓
录音动画 + 提示"请说出您的安排"
    ↓
用户说："明天下午3点开会"
    ↓
录音结束 → 调用语音识别
    ↓
识别文本："明天下午3点开会"
    ↓
本地规则引擎解析：
  意图 = add_event
  日期 = 2026-05-31
  时间 = 15:00
  事件 = 开会
    ↓
置信度 ≥ 阈值？
  是 → 写入数据库 → 语音播报"已添加明天下午3点的开会"
  否 → 语音确认"你说的是明天下午3点开会，对吗？"
        ↓ 用户确认 → 写入数据库
        ↓ 用户否定 → 提示重新输入
```

### 7.2 语音查看事件流程

```
用户说："今天有什么安排"
    ↓
意图 = query_event，日期 = 2026-05-30
    ↓
查询数据库 → 获取今日事件列表
    ↓
有事件？
  是 → 语音播报"今天有3个安排：上午10点项目评审，下午2点客户会议，下午5点团队周会"
  否 → 语音播报"今天没有安排"
```

---

## 八、云函数接口设计

### 8.1 voice-parse（语音解析）

**入参：**
```json
{
  "audioBase64": "录音base64编码（可选，使用第三方ASR时）",
  "text": "识别后的文本（使用微信插件时）"
}
```

**出参：**
```json
{
  "intent": "add_event",
  "entities": {
    "title": "开会",
    "date": "2026-05-31",
    "startTime": "15:00",
    "endTime": null,
    "reminder": null
  },
  "confidence": 0.92,
  "rawText": "明天下午3点开会"
}
```

### 8.2 event-manage（事件管理）

**添加事件：**
```json
// 入参
{ "action": "add", "data": { "title": "开会", "date": "2026-05-31", "startTime": "15:00" } }
// 出参
{ "success": true, "eventId": "xxx" }
```

**删除事件：**
```json
// 入参
{ "action": "delete", "eventId": "xxx" }
// 出参
{ "success": true }
```

**查询事件：**
```json
// 入参
{ "action": "query", "date": "2026-05-31" }
// 出参
{ "success": true, "events": [...] }
```

**修改事件：**
```json
// 入参
{ "action": "update", "eventId": "xxx", "data": { "startTime": "16:00" } }
// 出参
{ "success": true }
```

---

## 九、开发里程碑

### Phase 1 — MVP（核心可用）
- [ ] 小程序项目初始化 + 云开发环境搭建
- [ ] 月历视图 + 日视图 UI
- [ ] 手动添加/编辑/删除事件
- [ ] 语音录制 + 微信同声传译插件接入
- [ ] 本地规则引擎（基础意图解析 + 时间解析）
- [ ] 语音播报反馈
- [ ] 本地数据存储（wx.setStorage）

### Phase 2 — 增强
- [ ] 云数据库存储 + 多端同步
- [ ] 第三方ASR接入（百度/讯飞）
- [ ] 云函数NLP增强解析
- [ ] 微信订阅消息提醒
- [ ] 语音确认机制（低置信度时）
- [ ] 事件分类 + 颜色标签

### Phase 3 — 体验优化
- [ ] 语音动画效果优化
- [ ] 离线语音识别支持
- [ ] 语音快捷指令（如"清空今天的安排"）
- [ ] 事件重复设置（每天/每周/每月）
- [ ] 数据统计（本周安排数量等）
- [ ] 深色模式

---

## 十、你需要完成的事项清单

### 🔴 必须完成（开发前）

| 序号 | 事项 | 说明 | 预计耗时 |
|------|------|------|---------|
| 1 | 注册微信小程序账号 | 在 [mp.weixin.qq.com](https://mp.weixin.qq.com) 注册，获取 AppID | 30分钟 |
| 2 | 开通微信云开发 | 在小程序管理后台 → 云开发 → 开通（基础版免费） | 10分钟 |
| 3 | 安装微信开发者工具 | 下载 [开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html) | 15分钟 |
| 4 | 申请微信同声传译插件 | 小程序后台 → 设置 → 第三方设置 → 插件 → 搜索"同声传译"添加 | 10分钟 |
| 5 | 申请订阅消息模板 | 小程序后台 → 功能 → 订阅消息 → 添加"事件提醒"模板 | 15分钟 |

### 🟡 建议完成（增强功能）

| 序号 | 事项 | 说明 | 预计耗时 |
|------|------|------|---------|
| 6 | 注册百度AI开放平台账号 | 用于接入百度语音识别，[ai.baidu.com](https://ai.baidu.com) | 30分钟 |
| 7 | 创建百度语音识别应用 | 获取 API Key 和 Secret Key | 15分钟 |
| 8 | 安装 Vant Weapp | `npm i @vant/weapp -S --production`，用于UI组件加速 | 10分钟 |

### 🟢 可选完成（体验优化）

| 序号 | 事项 | 说明 | 预计耗时 |
|------|------|------|---------|
| 9 | 注册讯飞开放平台账号 | 备选语音识别方案，[xfyun.cn](https://www.xfyun.cn) | 30分钟 |
| 10 | 设计应用图标和启动页 | 1024x1024 图标 + 启动页图片 | 视设计而定 |

---

## 十一、风险与注意事项

| 风险 | 影响 | 应对方案 |
|------|------|---------|
| 语音识别精度不足 | 误添加/误删除事件 | 低置信度时强制确认；支持手动修正 |
| 微信同声传译插件限制 | 免费额度用尽 | 切换第三方ASR；本地缓存识别结果 |
| 订阅消息用户拒绝授权 | 无法推送提醒 | 小程序内提醒兜底；引导用户授权 |
| 小程序后台运行限制 | 无法精确到秒级提醒 | 使用云函数定时触发 + 订阅消息 |
| 语音解析歧义 | "3点开会"不知是上午还是下午 | 根据上下文推断；不确定时语音确认 |
| 隐私合规 | 语音数据敏感 | 本地处理优先；云端数据加密存储；隐私政策声明 |

---

## 十二、项目目录结构（最终）

```
calendar/
├── app.js
├── app.json
├── app.wxss
├── project.config.json
├── sitemap.json
├── package.json
├── cloudfunctions/
│   ├── voice-parse/
│   │   ├── index.js
│   │   └── package.json
│   ├── event-manage/
│   │   ├── index.js
│   │   └── package.json
│   └── reminder-check/
│       ├── index.js
│       └── package.json
├── components/
│   ├── calendar/
│   ├── voice-btn/
│   ├── event-card/
│   └── voice-feedback/
├── pages/
│   ├── index/
│   ├── day/
│   ├── event-detail/
│   ├── event-add/
│   └── settings/
├── utils/
│   ├── nlp.js           # 本地NLP解析引擎
│   ├── time-parser.js   # 时间解析工具
│   ├── voice.js         # 语音录制/播报封装
│   ├── event-store.js   # 事件数据操作封装
│   └── constants.js     # 常量定义
└── assets/
    ├── icons/
    └── images/
```
